const supertest = require('supertest');
import  app  from '../index.js' 

describe('Setup', () => {
    it('connects to the db', () => {

    })
})

describe('Auth', () => {
    it('/register -> puts user int db, returns the user and sets cookie', () => {

    })
    it('/login -> returns the user and sets cookie', () => {

    })
    it('/logout -> removes cookie', () => {

    }) 
})

describe('Users', () => {

})

describe('Projects', () => {

})

describe('Internships' , () => {

})
